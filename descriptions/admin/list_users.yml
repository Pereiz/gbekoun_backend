Liste tous les utilisateurs
---
tags:
  - Administration
security:
  - BearerAuth: []
parameters:
  - name: limit
    in: query
    type: integer
    default: 50
  - name: offset
    in: query
    type: integer
    default: 0
  - name: search
    in: query
    type: string
    description: Recherche par téléphone ou nom
  - name: status
    in: query
    type: string
    enum: [active, suspended, all]
    default: all
responses:
  200:
    description: Liste des utilisateurs